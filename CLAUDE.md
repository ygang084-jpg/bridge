@AGENTS.md

# BRIDGE SAFE — 화면과 경로

## 라우트

| 경로 | 화면 | 근거 디자인 |
| --- | --- | --- |
| `/` | 메인 대시보드 | Stitch `bridge_safe_9` |
| `/map` | **지도 화면** — 대시보드의 지도 영역을 누르면 여기로 온다 | Stitch `bridge_safe_5` |
| `/my-bridges` | 내 교량 (카드 목록) | Stitch `bridge_safe_5` |
| `/bridges` | F-01 내 주변 교량 목록 (위치·지역명 검색) | |
| `/bridges/[id]` | F-02 교량 상세 | |
| `/bridges/[id]/history` | F-03 관리 이력 타임라인 — 제품의 중심 | |
| `/bridges/[id]/today` | F-04 오늘의 상태 — 판정 로직 없음(§13 Q2·Q3 미해소) | |
| `/api/cron/refresh-summaries` | 자동 요약 갱신 (Vercel Cron) | |
| `/api/cron/refresh-news` | 교량 뉴스 수집 (네이버 검색 API, Vercel Cron) | |

### 내비게이션 (`src/components/AppNav.jsx`)

홈 `/` · 지도 `/map` · 내 교량 `/my-bridges` · 기록(대표 교량의 이력) · 내 정보(없음).
목적지가 없는 항목은 링크로 두지 않고 비활성으로 표시한다 — 눌러도 아무 일이 없는
링크는 고장으로 읽힌다. `/bridges`는 내비에 없고 홈의 CTA와 검색 카드에서 들어간다.

### 폭

`layout.jsx`의 `.app-shell`이 정한다. 기본 480px(단일 컬럼)이고, 본문에
`dashboard-wide` 클래스가 있는 화면만 1440px로 넓어진다 — `globals.css`의
`:has()` 규칙이다. 좁은 화면 5개는 데스크톱에서도 480px를 유지한다.

## 화면을 만들 때 지키는 것

Stitch 디자인 파일을 옮길 때 **레이아웃·카드 구성·색은 그대로 따르고, 값은 DB에서
읽는다.** 디자인 파일에 적힌 값(준공 2005년, 최근 점검 2026년 3월, 바람 2.4m/s,
A등급, 갱신 소식 4건 …)은 예시 텍스트이므로 옮기지 않는다. 실제 교량 이름에 지어낸
점검 기록을 붙이면 그대로 오정보가 된다.

값이 없을 때는 **자리를 남기고 왜 비어 있는지 적는다.** 지금 비어 있는 곳:

| 자리 | 이유 |
| --- | --- |
| 오늘의 상태 4타일 (강수·강풍·결빙·수위) | 출처 있는 기준치가 없다. 우리가 정하면 위험을 판정하는 것이 된다 (§13 Q2·Q3) |
| 통행 제한 여부 | `bridge_restrictions`가 비어 있다. 근거 없는 '제한 없음'은 정보 없음을 안전 신호로 바꿔 읽히게 한다 |
| 관측 3칸 (바람·온도·습도) | `weather_snapshots`가 비어 있다 |
| 구조 형식 · 사용 재료 | 담을 컬럼이 없다. `facility_type`(콘크리트교)은 다른 값이라 그 자리에 옮겨 적지 않는다 |
| 교량 사진 | 디자인 파일의 예시 이미지였다 |
| 즐겨찾기 · 교량 추가 | `favorites` 테이블도 로그인도 없다 (v1) |

만들지 않는 것: **대피 경로 안내**(오안내가 곧 인명 피해), **이상징후 제보**(접수 후
처리 경로가 없다), **로그인**(v0 비목표), **AI 요약**(요약은 규칙 기반 템플릿).

### 지도

지도는 카카오맵이고 SDK 를 부르는 곳은 `src/components/KakaoMap.jsx` 하나다. 이것을
쓰는 화면은 둘 — `/map`(`MapExplorer`, 조작 가능)과 대시보드의 지도 자리
(`MapPreview`, 끌기·확대 끄고 영역 전체가 `/map` 링크). **여기까지가 방문자 요청이
제3자로 나가는 범위다.** 다른 화면에 지도를 붙이면 그 범위가 넓어지는 것이므로 그때
다시 판단한다 (PRD §4 는 지도 렌더링을 v0 범위 밖에 두었고, 2026-08-19 에 뒤집었다).

지도를 못 띄우는 경우를 **두 화면 모두 다르게 말한다** — `/map`은 격자 배경 + 상대
위치 마커로, 대시보드는 회색 링크로 되돌리고 각각 왜 그런지 적는다. 빈 사각형은
'주변에 교량이 없다'로 읽힌다. 좌표가 없는 교량은 마커가 없으므로 대시보드는 몇 곳을
찍었는지(`교량 N곳`)를 함께 적는다.

`NEXT_PUBLIC_KAKAO_MAP_KEY` 는 이 저장소에서 `NEXT_PUBLIC_` 을 쓰는 **유일한** 값이다.
카카오 JS 키는 설계상 공개되며 보호 장치는 비밀이 아니라 개발자 콘솔에 등록한 사이트
도메인이다. REST 키를 여기 넣으면 안 된다 (도메인 제한이 없다).

### 등급 표시

안전등급 칩은 **중립 색**이고 문구는 등급 값만 적는다. 법정 정의 원문을 아직
확인하지 못했으므로(§13 Q4) 초록·노랑으로 칠하면 어느 등급이 좋은지를 색이 먼저
판정해 버린다. 실제로 표본의 C등급이 초록으로 나온 것을 렌더링해 보고 고쳤다.
등급의 제자리는 관리 이력 타임라인의 해당 시점이다 (§F-02.1).

### 뉴스

`bridge_news`는 네이버 뉴스 검색 API 결과 캐시다. 말머리(`[긴급]`·`[공지]` …)와
카테고리는 **제목·요약에 있던 낱말로만** 붙이고 맞은 낱말을 화면에 함께 보여준다
(`src/lib/news/classify.js`). 심각성을 우리가 판정하지 않기 때문이다. 요약은 기사
원문 발췌이며 새로 쓰지 않는다. 기사를 특정 교량에 붙이지 않는다 — '뉴스에 났다 =
위험하다'로 읽힌다.

## 코드 관례

- `src/lib/` 안에서는 상대경로 + `.js` 확장자로 import 한다 (`../history.js`).
  plain node 로 확인 스크립트를 돌리기 위한 것이며, `@/` 별칭은 앱 코드에서만 쓴다.
- 색·간격·타이포 토큰은 `src/styles/theme.css`(Stitch 원문)가 유일한 출처다.
  그 값을 다른 곳에서 덮어쓰지 않는다. Stitch 에 없는 것(상태 신호색, accent)만
  `globals.css`에서 정의한다.
- 아이콘은 `src/components/Icon.jsx`의 인라인 SVG를 쓴다. Material Symbols·Phosphor
  를 CDN 에서 부르지 않는다 (제3자 요청·CDN 장애).
- 외부 API 키는 서버에서만 읽는다. `NEXT_PUBLIC_` 접두사를 쓰지 않는다 — 예외는
  카카오 지도 JS 키 하나뿐이고 그 이유는 위 '지도' 절에 적었다.
